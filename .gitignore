__pycache__/
*.pyc
.DS_Store
# Upstream clones are reproducible via scripts/update_repos.sh — don't vendor them.
sources/repos/
# PRIVATE personal notes — never published. See private/README.md.
private/
# Cached vanilla game INIs (not published) — used by build_engine_string_surface.py
sources/ini-cache/
